import styled from "styled-components/native";
import React from "react";

interface Props {
  onBackPressed: () => void;
}

export default function CreatePlaceStage2(props: Props) {
  return <Container></Container>;
}

const Container = styled.View`
  flex: 1;
`;
