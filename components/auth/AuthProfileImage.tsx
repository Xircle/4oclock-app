import styled from "styled-components/native";
import React from "react";
import { View } from "react-native";

interface Props {}

export default function AuthProfileImage(props: Props) {
  return <View></View>;
}
